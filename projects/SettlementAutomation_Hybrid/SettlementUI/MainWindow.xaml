<Window x:Class="SettlementUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settlement Automation Premium" Height="650" Width="900"
        Background="#0F172A" WindowStyle="None" AllowsTransparency="True" 
        WindowStartupLocation="CenterScreen" BorderThickness="1" BorderBrush="#334155">
    
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Border Grid.Row="0" Background="#1E293B" MouseLeftButtonDown="TitleBar_MouseDown">
            <Grid>
                <TextBlock Text="SETTLEMENT ENGINE v2.0" Foreground="#94A3B8" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" FontSize="12" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="" FontFamily="Segoe MDL2 Assets" Background="Transparent" Foreground="#94A3B8" BorderThickness="0" Width="40" Click="Minimize_Click"/>
                    <Button Content="" FontFamily="Segoe MDL2 Assets" Background="Transparent" Foreground="#F43F5E" BorderThickness="0" Width="40" Click="Close_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="1" Padding="30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel: Execution & Log -->
                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="데이터 결산 자동화" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="로컬 폴더 및 클라우드 동기화 데이터를 감지하여 파워쿼리 최적화 형태로 정제합니다." Foreground="#64748B" FontSize="14" Margin="0,0,0,30" TextWrapping="Wrap"/>

                    <Border Background="#1E293B" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="대상 경로 설정" Foreground="#38BDF8" FontWeight="Bold" Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="PathTextBox" Grid.Column="0" Background="#0F172A" Foreground="White" BorderBrush="#334155" Padding="10" VerticalContentAlignment="Center" FontSize="13"/>
                                <Button Grid.Column="1" Content="경로 선택" Style="{StaticResource ModernButton}" Margin="10,0,0,0" Click="SelectPath_Click"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="실행 로그" Foreground="#94A3B8" FontWeight="Bold" Margin="0,0,0,5"/>
                    <Border Background="#020617" CornerRadius="8" BorderThickness="1" BorderBrush="#1E293B" Height="250">
                        <ScrollViewer x:Name="LogScrollViewer">
                            <TextBlock x:Name="LogText" Foreground="#10B981" FontFamily="Consolas" Padding="15" TextWrapping="Wrap" Text="대기 중..."/>
                        </ScrollViewer>
                    </Border>
                </StackPanel>

                <!-- Right Panel: Status & Actions -->
                <StackPanel Grid.Column="1">
                    <Border Background="#1E293B" CornerRadius="12" Padding="25" Margin="0,0,0,20">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="상태" Foreground="#94A3B8" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="StatusLabel" Text="준비 완료" Foreground="#10B981" FontSize="24" FontWeight="Bold" Margin="0,10"/>
                            <ProgressBar x:Name="ProgressBar" Height="6" Width="180" Background="#0F172A" BorderThickness="0" Foreground="#3B82F6" IsIndeterminate="False"/>
                        </StackPanel>
                    </Border>

                    <Button Content="자동 정제 엔진 가동" Style="{StaticResource ModernButton}" Height="60" Background="#3B82F6" Click="StartRefinement_Click">
                        <Button.Effect>
                            <DropShadowEffect Color="#3B82F6" BlurRadius="15" ShadowDepth="0" Opacity="0.4"/>
                        </Button.Effect>
                    </Button>
                    
                    <Border Background="#1E293B" CornerRadius="12" Padding="20" Margin="0,20,0,0">
                        <StackPanel>
                            <TextBlock Text="시스템 정보" Foreground="#64748B" FontWeight="Bold" Margin="0,0,0,10"/>
                            <TextBlock Text="• Python Engine: v3.14 (Pandas)" Foreground="#94A3B8" Margin="0,3"/>
                            <TextBlock Text="• Target: Excel, CSV" Foreground="#94A3B8" Margin="0,3"/>
                            <TextBlock Text="• Mode: Hybrid Automation" Foreground="#94A3B8" Margin="0,3"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
